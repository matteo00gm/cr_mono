{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests", ":semanticCommits"],
  "timezone": "Europe/Rome",
  "labels": ["dependencies"],
  "prConcurrentLimit": 3,
  "prHourlyLimit": 2,
  "rangeStrategy": "bump",
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 6am on monday"]
  },
  "packageRules": [
    {
      "description": "One weekly PR for every devDependency update. Individually they are noise; as a batch they are one review with one CI run.",
      "matchDepTypes": ["devDependencies"],
      "groupName": "devDependencies",
      "schedule": ["before 6am on monday"]
    },
    {
      "description": "Auto-merge patch, pin and digest updates to devDependencies once CI is green. Tooling patches are where the update cost exceeds the review value.",
      "matchDepTypes": ["devDependencies"],
      "matchUpdateTypes": ["patch", "pin", "digest"],
      "automerge": true,
      "platformAutomerge": true
    },
    {
      "description": "GitHub Action digests are pinned by P0-06 and kept fresh here. Safe to auto-merge: the digest is verified against the tag Renovate resolved it from.",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["digest", "pin", "patch"],
      "automerge": true,
      "platformAutomerge": true
    },
    {
      "description": "Never auto-merge a major. A major is a behaviour change by definition, and the whole value of the gate is that a human reads the changelog.",
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "addLabels": ["major"]
    },
    {
      "description": "Never auto-merge anything packages/security or packages/core depends on, at any update level. packages/core was added when P0-45 put the Better Auth configuration there: it now holds better-auth itself, which is in the security-critical path and belongs on a watch list rather than in any auto-merge group. Note this is belt and braces for production dependencies — the automerge rules above only cover devDependencies and Action digests — but the label is the point, so these updates arrive marked for a human rather than merely not-auto-merged.",
      "matchFileNames": ["packages/security/**", "packages/core/**"],
      "automerge": false,
      "addLabels": ["security-critical"]
    }
  ],
  "vulnerabilityAlerts": {
    "labels": ["security", "vulnerability"],
    "automerge": false,
    "schedule": ["at any time"]
  },
  "osvVulnerabilityAlerts": true
}
