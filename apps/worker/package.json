{
  "name": "@catalogorosso/worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1127.0",
    "@aws-sdk/client-sqs": "^3.1128.0",
    "@catalogorosso/core": "workspace:*",
    "@catalogorosso/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.4.0"
  }
}
